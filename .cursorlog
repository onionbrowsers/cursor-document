# Cursor 操作日志

---

## 2026-06-02 修复 Cron 会话标题与运行元信息展示

### 修改文件

- `src/lib/session-display-title.ts`
- `src/stores/chat.ts`
- `src/stores/chat/cron-session-utils.ts`
- `electron/utils/cron-session-projection.ts`
- `tests/unit/session-display-title.test.ts`
- `tests/unit/cron-history-display.test.ts`
- `tests/unit/session-index-scanner.test.ts`
- `tests/src/stores/chat-cron-history.test.ts`

### 修改说明

修复定时任务执行历史回放时，会话标题被 `[cron:<jobId> ...]` 运行包装污染或被过滤为空的问题；同时移除 cron fallback 投影中自动拼接到 assistant 正文末尾的“耗时/模型”运行元信息。

### 主要改动

- Cron 标题解析优先从 `[cron:<jobId> <任务名>]` 中提取任务名，避免使用后续 prompt 作为标题。
- Chat 历史加载时从“定时任务：xxx”元信息回填 session label，避免顶部标题为空。
- Cron run 投影不再把 `durationMs/provider/model` 拼接进 assistant 可见文本。
- 对旧索引/旧历史中的 `cron-run-*` assistant 消息做窄规则清洗，移除末尾完全由“耗时/模型”组成的运行元信息段。
- 更新相关单测覆盖标题解析、cron 历史展示和 session-index 期望。

### 追加修正

- 支持清洗只有单换行分隔的尾部运行元信息，例如 `正文\n耗时：132s | 模型：provider/model`。
- 在 `execution.finished.summary` 已经包含运行元信息时，也会在 main 侧 cron projection 源头剥离，避免重新进入 session-index 或 fallback 历史。

---

## [2026-03-03] Cursor Rules 规范体系整理

### 操作概述

将原 `.cursorrules` 文件中的所有规范内容，按照职责拆分为 `.cursor/rules/` 目录下的独立 `.mdc` 文件，并更新 `.cursorrules` 为精简的规则索引入口。新增两类规范（Git 提交规范、错误处理规范）作为体系补充。

---

### 一、新增/更新的规则文件

| 文件 | alwaysApply | glob 范围 | 内容描述 |
|------|-------------|-----------|----------|
| `ai-behavior.mdc` | `true` | 全局 | AI 助手身份、工作流程、项目管理、代码质量要求 |
| `chinese-language.mdc` | `true` | 全局 | 强制所有思考与回答内容使用中文输出 |
| `code-style.mdc` | `true` | 全局 | 通用编码原则、事件命名、CSS cursor 规范、异步代码规范 |
| `typescript-conventions.mdc` | `false` | `**/*.ts`, `**/*.tsx` | 命名前缀（I/E/T）、数组类型、文件命名、类型安全、工具类型、严格模式配置 |
| `react-conventions.mdc` | `false` | `**/*.tsx` | 组件结构目录、函数组件定义、Props 接口、Hooks 规范（useState/useEffect/useCallback/useMemo）、渲染规范、Next.js 补充规范 |
| `comment-conventions.mdc` | `false` | `**/*.ts`, `**/*.tsx` | JSDoc 注释规范（函数/变量/组件/Interface/Type/Enum）、行内注释规范、禁止事项 |
| `git-conventions.mdc` | `true` | 全局 | Conventional Commits 规范、分支命名规范、原子提交原则、禁止事项、Code Review 规范 |
| `error-handling.mdc` | `false` | `**/*.ts`, `**/*.tsx` | 异步错误处理、ApiError 类、React 组件三态管理、ErrorBoundary、表单校验错误处理 |
| `performance.mdc` | `false` | `**/*.tsx` | useMemo/useCallback/React.memo 使用规则、列表虚拟化、代码分割懒加载、图片优化、并行请求、请求缓存 |

---

### 二、更新 .cursorrules

将原有的完整规则内容精简为**规则索引入口**，包含：
- 规则文件列表（文件名、作用范围、描述）
- 技术栈说明
- 快速参考指引（场景 → 对应规则文件）

---

### 三、规则设计原则

- **全局规则**（`alwaysApply: true`）：影响所有文件的行为规范，如 AI 行为、代码风格、Git 规范
- **文件级规则**（`globs` 限定）：仅在特定文件类型激活，减少非相关文件的规则噪音
- **职责单一**：每个文件只覆盖一类规范，便于独立维护和更新

---

## [2026-03-03] 提分专项 PRD 飞书文档转 Markdown

### 操作概述
读取 `.cursor/docs/fe-reverse-teaching/提分专项/` 目录下所有反讲文档，提取其中的 PRD 飞书链接，通过飞书 MCP API 将 `docx` 类型文档转换为 Markdown 格式，并保存到 `.cursor/docs/product-requirement-document/提分专项/` 目录下。

---

### 一、创建 Cursor 规则

**文件**：`.cursor/rules/chinese-language.mdc`

**内容**：`alwaysApply: true`，强制要求所有 AI 思考过程与回答内容使用中文输出。

---

### 二、源文件目录

`.cursor/docs/fe-reverse-teaching/提分专项/`

读取该目录下所有 `.md` 文件，通过正则匹配提取 `**prd：**[标题](飞书链接)` 格式的 PRD 链接。

共提取到：
- 飞书 PRD 链接：**19 条**
- 非飞书链接（跳过）：**1 条**

---

### 三、成功转换的 PRD 文件（9 个）

保存目录：`.cursor/docs/product-requirement-document/提分专项/`

| 序号 | 文件名 | 飞书 Obj Token |
|------|--------|---------------|
| 1 | 收分V4.0+支持战略学科.md | （已转换） |
| 2 | 收分V4.1.md | （已转换） |
| 3 | 【已上线】EES内容交接+支持PDF试卷分析.md | （已转换） |
| 4 | 【已上线】企微收集成绩+AI作文批改.md | （已转换） |
| 5 | 【已上线】按学科调用模型分析&更新题目标签展示.md | （已转换） |
| 6 | 【已上线】阶段报告3.0.md | （已转换） |
| 7 | 【开发中】增加目标院校模块+补充埋点+数据准确性测试.md | （已转换） |
| 8 | 【已上线】试卷分析报告七期-线上报告替换+试卷分析功能优化+prompt优化.md | （已转换） |
| 9 | 【已上线】试卷分析报告八期-接入Theta能力.md | （已转换） |

---

### 四、跳过的 PRD（10 个）

以下 PRD 在调用 `user-feishu-mcp-wiki_v2_space_getNode` 时返回 `code: 131005, msg: not found`，无法解析 `obj_token`，已按规则跳过：

1. 【已上线】收分V3.0产品需求文档
2. 【已上线】收分模型升级V3.1
3. 【已上线】收分V3.2
4. 【已上线】分阶段报告1.0
5. 【已上线】阶段报告2.0
6. 【已上线】前端反讲-提分宝V1.1
7. 【已上线】前端反讲-提分宝V1.2
8. 【已上线】AI作文批改
9. （其他无法解析的节点）
10. （其他无法解析的节点）

> 跳过原因：飞书 `node_token` 无法通过 `getNode` API 解析为有效的 `obj_token`（文档可能已删除、迁移或权限不足）。

---

### 五、使用的主要 API

| API | 用途 |
|-----|------|
| `user-feishu-mcp-wiki_v2_space_getNode` | 将 `node_token` 解析为 `obj_token` 和 `obj_type` |
| `user-feishu-mcp-docs_v1_content_get` | 获取飞书 `docx` 文档内容并转换为 Markdown |

---

### 六、执行结果汇总

- 总 PRD 链接数（飞书）：19
- 成功转换：**9**
- 跳过（getNode not found）：**10**
- 跳过（非飞书链接）：**1**

---

## 2026-03-05 补充反讲文档第5、6大点内容

### 修改文件

`.cursor/docs/fe-reverse-teaching/中价课/【前端反讲】真题考点·好题速递.md`

### 修改说明

基于 `feature-20260304-middleCourse` 分支的 `git diff`，对反讲文档的第5大点（设计思路和方案）与第6大点（详细设计）进行了内容补充和代码注释完善。**仅修改了这两个大块，第1-4章及第7章保持原样。**

### 第5大点改动

| 小节 | 改动内容 |
|------|---------|
| 5.1 整体流程图 | 将原文字列表替换为 Mermaid `flowchart TD` 流程图，完整展示从首页入口 → 列表页 → 解析页 → CollapsibleActionBar → 跳转/加好友的交互路径 |
| 5.3 整体项目难点梳理与解决方案 | 填充原为空的表格，补充4条技术难点及对应解决方案（定时器管理、横竖屏响应式、跨平台路由、Mock数据防空白） |
| 5.4 跨部门协同方案（新增） | 新增该小节，明确与后端（接口约定）、产品/后端（`recommendReason` 字段）、数据侧（Habo 事件 ID 及参数）的协作约定 |

### 第6大点改动

| 小节 | 改动内容 |
|------|---------|
| 6.1.2 好题速递列表页 | 补充代码片段：`useChangeTitle` 标题变更、`MIDDLE_RECOMMEND_ITEM_CLICK` 埋点新增 `question_id`、注释掉版本切换按钮代码 |
| 6.1.3 题目解析页 | 将 `FloatGuideButton` 描述更正为 `CollapsibleActionBar`；补充完整代码实现（state 定义含 Mock 数据、`getCollapsibleActionBarContent` 接口调用、`pageJump` 三端路由跳转含埋点、CollapsibleActionBar JSX 渲染） |
| 6.2 通用代码设计 | 将 `FloatGuideButton` 节替换为 `CollapsibleActionBar` 节，补充完整 TypeScript 实现（含 JSDoc 注释、`ICollapsibleActionBarProps`、`isOpen` state、`timerId` ref、`useEffect` 自动折叠逻辑、JSX 三区块注释）；新增相关类型定义（`IFunctionEntries`、`IGetCollapsibleActionBarContentInput/Output`）及 Service 层封装（`getCollapsibleActionBarContent`），所有代码均附中文 JSDoc 注释 |
| 6.3 接口描述 | 新增 `GET /api/practice/get-collapsible-action-bar-content` 接口；已有接口描述中将 `question_id` 字段更正为 `recommendReason` 字段 |

### 代码注释规范

本次所有补充的代码片段均遵循项目注释规范：
- 函数/组件使用 `/** */` JSDoc 格式，包含 `@description`、`@param`、`@return`、`@example`
- state/ref 变量使用 `@description` + `@type` 注释
- interface 字段使用行内 `/** */` 注释说明用途
- 关键逻辑节点补充行内中文注释，说明非显而易见的意图

---

## 2026-06-02 新增 Cursor 配置中心 README

### 修改文件

`README.md`

### 修改说明

新增 `.cursor` 独立仓库的说明文档，介绍配置中心定位、目录结构、Rules/Commands/Skills/Hooks 的职责边界，以及跨项目复用的推荐方案。

### 主要内容

- 说明 `.cursor` 仓库作为跨项目 Cursor 配置中心的用途。
- 梳理 `rules/`、`commands/`、`skills/`、`hooks/`、`scripts/`、`disabled/` 等目录职责。
- 推荐使用 Git Submodule 在多个项目中复用同一套 `.cursor` 配置。
- 补充日常维护流程、禁用归档规则、安全与提交规范、常见问题。

---

## 2026-06-02 新增个人全局 Cursor 软链接初始化 Skill

### 修改文件

- `/Users/mac/.cursor/skills/cursor-shared-config-link/SKILL.md`
- `/Users/mac/.cursor/skills/cursor-shared-config-link/scripts/install-shared-cursor.sh`
- `.cursor/README.md`

### 修改说明

新增用户级全局 Skill `cursor-shared-config-link`，用于在新项目中一键安装共享 Cursor 配置。该 Skill 会调用脚本，将当前项目的 `.cursor` 软链接到本机中央配置目录 `~/.cursor/shared-config`。

### 主要能力

- 默认从 `https://github.com/onionbrowsers/cursor-document.git` 初始化中央配置目录。
- 当前项目不存在 `.cursor` 时，自动创建 `.cursor -> ~/.cursor/shared-config` 软链接。
- 当前项目已有 `.cursor` 时默认不覆盖；需要替换时需显式设置 `CURSOR_LINK_REPLACE=1`。
- 中央配置目录存在未提交改动时，跳过自动 pull，避免覆盖本地变更。
- README 补充了个人单机软链接方案的使用说明。

---

## 2026-06-02 加固 Cursor Hook 软链接识别逻辑

### 修改文件

- `hooks/after-file-edit-sync.sh`
- `scripts/README.md`

### 修改说明

调整 `afterFileEdit` 自动提交脚本，使其兼容项目 `.cursor` 软链接到 `~/.cursor/shared-config` 的场景，并明确只在 `.cursor` 配置仓库内执行 `git add`、`commit`、`push`。

### 主要改动

- 增加 `realpath` 解析，识别软链接真实路径。
- 优先识别 `~/.cursor/shared-config`，避免该路径中的 `/.cursor/` 被误判为项目 `.cursor`。
- 保留项目内 `.cursor` 路径识别，兼容非软链接独立仓库。
- 增加 Cursor 配置仓库特征校验，避免误提交业务项目仓库。
- `.needs-capability-update` 写入 `.cursor` 配置仓库根目录。
- 补充脚本文档，说明 hook 兼容软链接共享配置场景。

---

## 2026-06-02 优化 Cursor 配置自动同步 Hook

### 修改文件

- `hooks/after-file-edit-sync.sh`
- `scripts/git-hooks/post-commit`

### 修改说明

定位 `.cursor` 自动同步到 GitHub 偶发慢和超时的问题，主要风险来自文件编辑 hook 与
Git post-commit hook 都会后台执行 `git push`，频繁编辑时容易产生多个并发推送。

### 主要改动

- 保留自动 `push` 行为，不改为只 commit。
- 新增 `.git/cursor-sync.lock` 目录锁，同一时间只允许一个同步任务运行。
- 新增 `CURSOR_SYNC_DEBOUNCE_SECONDS` 防抖窗口，默认等待 20 秒后汇总提交。
- 新增 `CURSOR_SYNC_PUSH_TIMEOUT_SECONDS` 推送超时配置，默认 120 秒。
- 新增 `.git/cursor-sync.log` 本地日志，使用 ASCII 文案记录触发来源、commit、push 成功/失败与超时信息，避免不同 hook 环境下日志编码异常。
- `afterFileEdit` 与 `post-commit` 共用同一锁和日志，避免两个入口互相抢推。

---

## 2026-06-03 智能体卸载通道转绑提示与后端转绑

### 改动说明

- `AgentUninstallDialog`：若智能体存在通道绑定（含 `agent.bindings` 或微信已绑定），卸载确认文案改为提示通道将自动转绑到 YonClaw；无绑定时保留原文案。
- `deleteAgentConfig`：删除智能体时，将可转移的通道绑定（微信账号级、非 `agent:` 友空间账号）显式 `ensureChannelBindingToAgent('main', ...)`，修复 UI 显示未绑定但微信仍路由到 main 的不一致。
- 测试：`agent-config.test.ts`、`agent-market-frontend.test.tsx` 各补 1 条用例。

---

## 2026-06-03 定时任务「任务时间」i18n 缺失修复

### 改动说明

- 在 `src/i18n/locales/{zh,en,zh-TW,ja}/cron.json` 的 `schedule` 下补齐 `cronExpression`、`multiHoursDaily`、`multiMonthsDaily`、`multiMonthDaysAt`；`zh-TW` 另补 `intervalOnWeekdaysMinutes` / `intervalOnWeekdaysHours`。
- 根因：`formatCronExpressionLabel` 对 `raw` 等分支已调用 `t('schedule.cronExpression')`，缺 key 时 i18next 回退为 key 名；未改 `cron-helpers.ts` 逻辑。
- 新增 `tests/unit/cron-i18n-schedule-keys.test.ts` 防止 locale 再次漏配。

---

## 2026-06-03 侧栏搜索对齐 MasterGo 713:011925

### 改动说明

- `Sidebar.tsx`：搜索框去掉描边/清除钮与蓝色 focus 描边；背景 `rgba(255,255,255,0.8)`；占位「搜索」与 `(⌘+F）` 左右分布、色值 `#D0D0D0`；展开侧栏时 `⌘/Ctrl+F` 聚焦搜索输入；保留左侧放大镜图标，与「搜索」文案间距 9px。

---

## 2026-06-03 侧栏导航 796:82851 / 610 四宫格对齐

### 改动说明

- `Sidebar.tsx`：`NavItem` 脱离 `GlassNavItem`，扁平行 32px、13/18；hover/active `#E2E2E2`；新建任务去磨砂与 ⌘K 徽标；智能体市场改用 `CHANNEL_NAV_ICON`。
- 新增 `newTaskNavIcon.tsx`、`agentMarketNavIcon.tsx`；`mastergo-nav-icons-data.ts` 增加 `NEW_TASK_NAV_ICON`。
- 分步记录：`.cursor/plans/sidebar-nav-610-step-log.md`；单测 `sidebar-navigation.test.tsx` 导航/搜索断言同步。
- 补正 Step G：定时/技能/市场改用 796 DSL path（`732:81560/61`、`732:81571`、`732:81568`），不再用 289 闹钟/四宫格/CHANNEL 图标。

---

## 2026-06-03 侧栏智能体会话区 802:84337

### 改动说明

- 新增 `SidebarAgentPanel`、`SidebarSessionItem`、`SidebarSessionMoreMenu`（`src/components/layout/sidebar/`）。
- 802 图标 path：`sidebar-802-icons-data.ts`；时间格式：`format-sidebar-session-time.ts`（含「N周」）。
- 会话置顶：`sidebar-ui` store `pinnedSessionKeys` 持久化；hover/active 显示置顶/重命名/删除。
- `Sidebar.tsx` 接入组件，移除分组头设置按钮，保留折叠；虚拟列表支持可变行高。
- 分步记录：`.cursor/plans/sidebar-agent-panel-802-step-log.md`

---

## 2026-06-04 侧栏底部头像与设置图标（713:011911 / 713:012347）

- `SidebarFooterAvatar`：24×24、圆角 4px、描边 `border-white/80`；仍用 `Avatar` + 用户头像/文字
- `SidebarFooterSettingsIcon`：MasterGo 齿轮 SVG 替换原 `settingsGlyphIcon`
- 单测 `sidebar-navigation` footer 用例与 mock 补齐 `pinnedSessionKeys`

---

## 2026-06-03 侧栏会话行交互修复（Step 5）

### 改动说明

- `SidebarSessionItem`：整行 hover 灰底；右侧固定 76px 槽切换时间/操作，去掉绝对定位重叠；操作 icon 无单独 hover 底。
- `SidebarSessionMoreMenu`：移除 `visible` 门控（修复 hover 不显示三点）；`destroyOnClose`。
- `Popup.css`：关闭态 positioner `pointer-events: none`。
- `SidebarAgentPanel`：头部 `items-center`。
- `Sidebar.tsx`：会话行高 40px；展开更多去掉 hover 灰底。

---

## 2026-06-03 定时任务复杂 Cron 语义化展示

### 改动说明

- `schedule-builder`：新增 `minuteStepHourRangeWeekdays`（如 `*/10 9-18 * * 1-5`）、完善 `hourRangeWeekdays`（如 `0 9-18 * * 1-5`）分类；导出 `parseCronMinuteStepField` / `parseCronHourRangeField` / `parseCronWeekdayField`。
- `cron-helpers`：`formatDisplayOnlyCronLabel` 输出自然语言，例如「周一至周五 9:00 上午至6:00 下午 每 10 分钟」。
- i18n：`hourRangeOnWeekdays`、`minuteStepHourRangeOnWeekdays`（四语言）；仍保留 `cronExpression` 作无法解析时的兜底。

---
