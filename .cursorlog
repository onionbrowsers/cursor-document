
## 2026-07-15 修复 webview_call_page_action 成功路径 ReferenceError

### 问题
`packages/openclaw-webview-agent/index.ts` 在 dispatch 成功且存在 `innerPayload` 时，误用未声明变量 `action` 调用 `summarizePageActionPayload`，导致 `ReferenceError: action is not defined`，模型侧看到工具报错。

### 修复
- 改为 `summarizePageActionPayload(String(params.action), innerPayload)`
- 新增单测覆盖 nested dispatch payload（`agent:cockpit-create-prepare`）成功摘要路径


### 需求
`yonbip-ec-file-fe` / `iuap-ap-workbenchui-fe` 顶层静态 import 在联邦缺失或组件抛错时会整页白屏。

### 实现
- 新增 `createSafeLazyRemote`：React.lazy + Suspense + ErrorBoundary + loadKey 可重试
- 新增 `lib/remote/modules.tsx` 集中导出 `EmbeddedKnowledge` / `CockpitWorkbenchApp`
- 改造 Cockpit、AgentBridgeDemo、KnowledgeBase、KnowledgeBasePickerDialog
- i18n 增加 `remoteModule.loading|loadFailed|loadFailedHint|retry`

## 2026-07-14 knowledge-base bootstrap 增加 onlySelectDir

- `packages/agent-bridge-core/src/knowledge-base.ts`：类型与 `buildKnowledgeBaseHostSelectionState` 写死 `onlySelectDir: false`
- 对齐 yonclaw1.0；随 BOOTSTRAP / STATE 下发

## 2026-07-14 knowledge-base bootstrap 增加 canSelectDir

- `packages/agent-bridge-core/src/knowledge-base.ts`：类型与 `buildKnowledgeBaseHostSelectionState` 写死 `canSelectDir: true`
- 对齐 yonclaw1.0；BOOTSTRAP / STATE 经 `use-knowledge-base-host` 自动带上

## 2026-07-15 驾驶舱上下/左右布局切换保留输入草稿

### 根因
`WebviewSessionWorkbench` 在 `sessionOpen` 切换时于底部 Dock 与侧栏会话各挂一套 `ChatComposer`，本地 input 随卸载丢失；`keepSessionMounted` 时还会双挂导致 ref 错乱。

### 修复
- Workbench 提升草稿并通过 `onInputValueChange` / 包装 `setValue` 同步
- 布局切换 `useLayoutEffect` 回填活跃 Composer
- 会话面板 `showComposer={sessionOpen}`，始终只挂载一个输入框

## 2026-07-14 WebviewDemo 增加预填测试按钮

- `html/webviewDemo/index.html` 新增「预填输入框（不发送）」按钮
- 触发 `agent:webview:send-prompt` + `autoSend: false`，用于验收本次预填改动

## 2026-07-14 send-prompt 增加 autoSend 预填参数

### 需求
页面通过 `agent:webview:send-prompt` 可将文案放入宿主输入框，而不立即发送。

### 实现
- 协议新增 `autoSend?: boolean`（默认 true）及 `resolveWebViewSendPromptAutoSend`
- `ResolvedSendPromptEvent` / route / generic actions 透传 `autoSend`
- Cockpit / WebviewDemo：`autoSend=false` → `setValue` + focus + 可选开浮层
- Chat WebView runtime：经 `fillComposer` 回调写入 ChatInput
- 文档与单测已同步

### 页面用法
```js
emit("agent:webview:send-prompt", {
  prompt: "请帮我分析当前页的关键指标。",
  autoSend: false,
  openPanel: true
});
```

## send-prompt autoSend 预填对齐 yonclaw1.0

- packages/agent-bridge-core: WebViewAgentSendPromptPayload.autoSend + resolveWebViewSendPromptAutoSend
- packages/agent-bridge-host: ResolvedSendPromptEvent / route / generic-actions 透传 autoSend
- apps/claw-web Cockpit: autoSend=false 时 pending 预填 Composer（兼容分屏切换挂载）
- CockpitChatSession: 支持父级 composerRef

## 2026-07-14 composer generating diagnostics
- 新增 src/lib/composer-generating-diagnostics.ts：判定「可见回复但仍 UI 生成/锁输入」并写入 flow-trace
- 新增 src/hooks/use-composer-generating-diagnostics.ts：Chat / Cockpit 共用 hook
- 接入 src/pages/Chat/index.tsx、useChatConversationModel.ts
- 增强 chat.abort / stuck recovery / stuck finalize 的 flow-trace 字段
- 单测 tests/unit/composer-generating-diagnostics.test.ts

## 2026-07-14 knowledge-base bootstrap canSelectDir
- shared/knowledge-base.ts: KnowledgeBaseBootstrapKnowledgeBaseSection 新增 canSelectDir
- use-knowledge-base-bridge.ts: buildKnowledgeBaseHostSelectionState 写死 canSelectDir: true（随 BOOTSTRAP / STATE 下发）
- 单测与 selection-bridge / 二方包说明文档已同步

## 2026-07-14 修复驾驶舱多语 UID 冲突

- `apps/claw-web/src/i18n/source/common.ts`：`sidebar.cockpit` 与 `cockpit.*` 原先误复用 `actions.load~skip` 的 UID（0458010B~04580112），平台词条显示为「加载」等错误文案
- 改为中文原文直出，待多语抽取工具分配新 UID；`actions.*` 保持原 UID 不动

## 2026-07-14 cursor-capability-check
- 触发：.needs-capability-update（changed_file=.cursorlog，2026-07-14 20:08:23）
- 重写 .cursor/cursor-capabilities.md：commands 5 / rules 10(always 5) / skills 7 / agents 0 / hooks 2 脚本 / scripts 若干
- 清理过时条目（飞书/TAPD commands、agents、旧 skills）；标注同步脚本与 README 漂移
- 已删除 .needs-capability-update

## 2026-07-14 knowledge-base bootstrap onlySelectDir
- bootstrap/state knowledgeBase 新增 onlySelectDir，写死为 false（只能选文件夹；默认关闭）
- 同步类型、单测与桥接文档

## 2026-07-15 修复驾驶舱停止后清除按钮一直禁用

### 原因
- 清除按钮依赖 `isSessionIdle`（store 的 `activeRunId`/`sending` 等）
- 输入框停止后靠本地 `abortedReplyRun` 立即解锁
- abort 后 SSE `commitStreamRunId` 仍会写回 `activeRunId` 并清除 abort 屏障，造成「能输入但不能清除」

### 改动
- 新增 `lib/chat-stream-run-commit.ts`：abort 后拒绝复活 runId
- `stores/chat.ts`：`commitStreamRunId` 与 send 成功路径接入拒绝逻辑
- `resetCockpitSessionHistory`：先 abort 再判 idle
- `use-cockpit-session-recovery`：显式订阅 run 状态驱动 `isSessionIdle`
- 补充单测

## 驾驶舱上下/左右布局切换保留 Composer 草稿

- ChatInput: 新增 onInputValueChange
- CockpitChatSession: composerDraftRef + callback ref 包装 + sessionOpen useLayoutEffect 回填
- ChatSessionSurface: showComposer，关闭会话栏时不挂载 Composer，避免与 Dock 双实例抢 ref


## cursor-capability-check（2026-07-15）

- 触发：`.needs-capability-update`（changed_at=2026-07-15 17:42:15）+ 用户确认「运行」
- 更新：重写 `.cursor/cursor-capabilities.md`
- 清理：删除 `.cursor/.needs-capability-update`
- 扫描结果：5 commands / 10 rules（5 alwaysApply）/ 7 skills / 0 agents / 2 hooks / 4 scripts
- 重叠：README 与 after-file-edit-sync 文档漂移等 3 处（较上次无新增）
## 2026-07-15 18:10
### 用户问题
1 + 1
### 处理说明
简单算术提问，无需代码改动，直接给出答案 2。
## 2026-07-16 20:25
- 修改 apps/claw-web/tailwind.config.cjs：关闭 corePlugins.preflight
- 原因：cssSandbox 前缀 .YonclawCloudWeb 抬高 Preflight 标签选择器优先级，压过二方包 CSS Module
## 2026-07-16 20:34
- 新增 apps/claw-web/scripts/postcss-sandbox-where-type.cjs：将 .YonclawCloudWeb + 标签选择器降为 :where(.YonclawCloudWeb)
- 修改 apps/claw-web/yds.config.js：webpack environment 钩子在 TNS prefixwrap 之后追加该 PostCSS 插件
- 目的：保留 Tailwind Preflight（主站 UI），同时避免压过二方包 CSS Module
