---
description: 多需求确认、分步开发与验收指引 - 定义 AI 在复杂任务中的工作流程
alwaysApply: true
---

# 开发工作流程规范

## 多需求输入：先澄清，再开发

当用户一次提出**多条需求**、**多个功能点**或**复合任务**时：

- **禁止**在未充分理解前直接编写代码
- **必须**先完整阅读并归纳全部需求点，输出简短的需求理解摘要
- **必须**将不明确、有歧义、存在多种实现方案或可能互相冲突的点**逐条向用户确认**
- 仅在需求已足够明确、且无待决问题时，再进入计划与开发阶段

确认时可参考以下维度（按需选用，不必机械罗列）：

- 本次是否全部做完，还是分阶段交付？
- 存在冲突时，优先级如何排序？
- 是否需要兼容现有行为或保留回退路径？

## 大型改动：分步规划，按序执行

当任务属于**大型改动**或**多模块需求开发**（例如：跨 3+ 文件、涉及架构/接口变更、或用户明确标注为多步骤）时：

- **必须**先输出分步计划（Step 1、Step 2…），说明每步目标、涉及文件与验收点
- **必须**按步骤顺序执行：完成当前步并确认无阻塞后，再进入下一步
- **禁止**跳过计划、一次性并行改完所有相关文件
- 若使用 Plan 模式，将计划保存至 `.cursor/plans/`；执行过程中可维护 step-log（参考既有 `*-step-log.md` 格式）

单步内若发现计划需调整，应先说明原因并更新后续步骤，再继续执行。

## 大型改动完成：提供精简验收指引

大型改动或分步任务**全部完成后**，在回复末尾提供简短的**验收 / 排查指引**：

- 控制在几句话，**不要**长篇复述实现细节
- 列出 2–5 条用户可自行 check 的操作（如：启动命令、访问路径、关键交互流程）
- 若出现问题，明确告诉用户应向 AI 提供什么以便排查，例如：
  - 相关**文件路径**
  - **终端日志**或**浏览器 Console** 报错全文
  - **复现步骤**与预期/实际结果
  - 必要时附上 `pnpm run typecheck`、`pnpm test` 等命令输出

重点是「怎么验」和「出问题带什么回来」，而非重复代码说明。

## 会话改动追踪与独立审查

- 开发会话中的文件改动由 Hook 自动写入 `.cursor/reviews/last-session-manifest.md`（见 `.cursor/reviews/README.md`）
- 大型任务完成后，除验收指引外，可在 step-log 中补充变更说明，便于后续 `/review-change` 审查
- **独立审查**应在新会话执行 `/review-change`，不要在同一轮开发会话末尾混做审查
