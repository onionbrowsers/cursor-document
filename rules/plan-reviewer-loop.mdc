---
description: Plan 审查闭环 - SubAgent 挑刺，主 Agent 必须甄别后给推荐方案（禁止原样转发）
alwaysApply: true
---

# Plan Reviewer 闭环

适用：用户触发 `plan-reviewer` /「审一下这个 plan」，或审查循环进行中。

## 角色

- **SubAgent `plan-reviewer`**：只读挑刺，输出原始 FAIL/PASS 与 Fi；不改文件；不做面向用户的取舍
- **主 Agent**：必须对 Fi **重新裁决**后再对用户说话；禁止传声筒

## 对用户回复的强制模板（缺任一节 = 不合规，须先补全再问确认）

收到 SubAgent 结果后，用户可见回复**必须**使用下列标题（可中英其一，但节不能缺）：

### 1. 审查原判
一行：`SubAgent VERDICT` + blocker/major/minor 数量。不要贴长表。

### 2. 主会话裁决（核心）
对**每一条** SubAgent 问题给出裁决，格式固定：

- `F#: 必改 | 可缓 | 不采纳` — 一句话理由（对照用户需求 / 已冻结项 / 代码证据）
- 若标「必改」：再写「建议写入 plan 的具体改法」（1–3 句）
- 若标「可缓/不采纳」：写清为何本阶段不必进 plan

**禁止**把 SubAgent 的 major/blocker 默认当成必改。你必须自己想过；若全部升级为必改，须在本节末写：「为何没有可缓项」的总理由（至少 2 句）。

### 3. 推荐落盘方案（仅一份）
只列出你裁决为 **必改** 的项，合并成准备写入 plan 的变更摘要。不要把可缓项混进这份方案。

### 4. 确认问句（唯一允许的问法）
只问：是否按 **§3 推荐落盘方案** 更新 plan？  
允许用户改单条裁决。  

**禁止**的问法示例：
- 「是否按上述拟修改点更新 plan？」
- 「全部按建议改 / 只改 F1–F3？」
- 把 SubAgent 建议原样打包成一个 yes/no

## 循环

1. 调 `plan-reviewer` → 2. 按强制模板回复并等待确认 → 3. 仅写入用户确认的必改项 → 4. 再调审查  
最多 5 轮；`PASS` 后先问是否开工，未确认不写业务代码。

## 禁止

- 未经确认改 plan / 改代码
- 只总结 SubAgent、不做「必改|可缓|不采纳」逐条裁决
- 用「按审查建议改」代替主会话自己的推荐方案
