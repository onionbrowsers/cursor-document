---
description: 注释规范 - 适用于所有 TypeScript 和 TSX 文件的 JSDoc 注释标准
globs: ["**/*.ts", "**/*.tsx"]
alwaysApply: false
---

# 注释规范

## 函数注释

函数注释必须使用 JSDoc 格式，包含以下必填项：

- `@description` - 描述函数用途
- `@param` - 描述每个参数（名称、类型、含义）
- `@returns` - 描述返回值
- `@example` - 提供完整的调用示例（含输入与输出）

```ts
/**
 * @description 格式化日期为 YYYY-MM-DD 字符串
 * @param date - 需要格式化的日期对象
 * @param separator - 日期分隔符，默认为 "-"
 * @returns 格式化后的日期字符串
 * @example
 * // 输入
 * formatDate(new Date("2024-01-15"), "-")
 * // 输出
 * "2024-01-15"
 */
const formatDate = (date: Date, separator = "-"): string => {
  // ...
};
```

**规则：**
- 注释必须放在函数上方，与函数之间**不空行**
- 注释必须使用 `/** */` 格式，不使用 `//` 或 `/* */`
- 参数名不加类型标注（TypeScript 类型已在函数签名中声明）

---

## 变量注释

变量定义须足够语义化，对于非显而易见的变量添加注释。

```ts
/**
 * @description 用户鉴权 token，存储在内存中，页面刷新后失效
 * @type {string}
 */
const authToken = "";

/**
 * @description 最大重试次数，超过后停止轮询
 * @type {number}
 */
const MAX_RETRY_COUNT = 3;
```

**规则：**
- 注释使用 `@description` 说明变量用途
- 使用 `@type` 说明变量类型（即使 TypeScript 可以推导，也建议在公共配置变量上注明）
- 注释放在变量上方

---

## 组件注释

React 组件必须在函数定义上方添加 JSDoc 注释。

```tsx
/**
 * @description 用户头像组件，支持图片 URL 和文字缩写两种展示模式
 * @param props.src - 头像图片 URL，为空时展示 name 的首字符缩写
 * @param props.name - 用户名称，用于生成文字缩写和 alt 属性
 * @param props.size - 头像尺寸，单位 px，默认 40
 * @returns 渲染后的头像节点
 */
const UserAvatar = ({ src, name, size = 40 }: IUserAvatarProps) => {
  // ...
};
```

**规则：**
- 必须使用 `/** */` 格式，放在组件函数上方
- 使用 `@param props.xxx` 描述每个 Props 字段，而非笼统的 `@param props`
- 使用 `@returns` 描述组件返回内容

---

## TypeScript 类型注释

Interface、Type Alias、Enum 均需添加注释。

### Interface

```ts
/**
 * @description 用户基本信息接口
 */
interface IUserInfo {
  /** 用户唯一标识 */
  id: string;
  /** 用户昵称 */
  name: string;
  /** 用户头像 URL */
  avatarUrl?: string;
}
```

### Type Alias

```ts
/**
 * @description 分页请求参数类型
 */
type TPaginationParams = {
  /** 当前页码，从 1 开始 */
  page: number;
  /** 每页数量 */
  pageSize: number;
};
```

### Enum

```ts
/**
 * @description 用户角色枚举
 */
enum EUserRole {
  /** 普通用户 */
  User = "user",
  /** 管理员 */
  Admin = "admin",
  /** 超级管理员 */
  SuperAdmin = "super_admin",
}
```

**规则：**
- 类型定义使用 `/** */` 注释放在定义上方
- Enum 的每个枚举值必须有行内 `/** */` 注释说明用途
- Interface 字段使用行内 `/** */` 注释

---

## 行内注释规范

- 行内注释使用 `//`，放在代码上方（不放在代码行末尾）
- 只注释**非显而易见**的逻辑，不注释自描述代码
- 禁止"叙述性注释"：不写 `// 定义变量`、`// 返回结果`、`// 导入模块` 等

```ts
// ✅ 好的注释：说明非显而易见的边界条件
// 后端返回的 timestamp 为秒级，需转换为毫秒
const date = new Date(timestamp * 1000);

// ❌ 禁止叙述性注释
// 获取用户列表
const users = await fetchUsers();
```

---

## 禁止事项

- 禁止在代码中留下 TODO、FIXME 而不附带 issue 或 ticket 编号
- 禁止注释掉大段代码提交到主分支，应直接删除后由 git 历史追溯
- 禁止使用注释解释代码修改内容（应由 git commit message 承载）
