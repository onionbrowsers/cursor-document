---
description: 前端反讲文档编写规范 - 定义反讲文档的结构、内容、代码、接口、埋点等写作标准。当用户要求编写前端反讲文档、fe-reverse-teaching 文档、反讲文档时应用此规范。
alwaysApply: false
---

# 前端反讲文档编写规范

## 文件命名与存放

- 文件名格式：`【前端反讲】<产品文档名>.md`
- 存放路径：`.cursor/docs/fe-reverse-teaching/<项目名>/`
- 示例：`.cursor/docs/fe-reverse-teaching/中价课/【前端反讲】真题考点·好题速递.md`

---

## 文档结构总览

```
# 【前端反讲】<产品文档名>
# 修改记录
# 1 项目背景
# 2 名词解释
# 3 设计目标
# 4 系统环境
# 5 设计思路和方案
# 6 详细设计
# 7 埋点信息
```

---

## 各章节编写规则

### 修改记录

- 使用 Markdown 标准表格，**禁止使用 HTML 标签格式（`<table>`）**
- 表头：版本号 / 修改日期 / 作者 / 修改说明

```markdown
| 版本号 | 修改日期 | 作者 | 修改说明 |
| --- | --- | --- | --- |
| v1.0 | 2026-03-05 | 前端开发 | 初版创建 |
```

---

### 1 项目背景

#### 1.1 背景介绍
- 说明本次需求的业务背景、核心改动点（P0/P1/P2 分级标注）

#### 1.2 文档资料
- **PRD**：若用户提供的是 doc ID（如 `TL87wM2zvi8LZJkoLNdchi5tnDf`），需通过 feishu-mcp 获取完整飞书链接后写入
- **UI 设计稿**：从 PRD 文档内容中提取 Figma 链接，若 PRD 中未提及则留空
- **Git 仓库**：从项目代码路径或已知配置中获取
- **TAPD 需求**：用户提供则写入，未提供则填「待补充」

#### 1.3 项目关联方
- 使用 Markdown 标准表格，**禁止使用 HTML 标签格式**
- 表头：角色 / 姓名或团队 / 职责

---

### 2 名词解释

- 使用 Markdown 标准表格，**禁止使用 HTML 标签格式**
- 仅列出文档中涉及的业务专有名词，通用技术术语无需解释

---

### 3 设计目标

- **3.1 功能目标**：从 PRD 提取，按编号列出，标注 P0/P1/P2 优先级
- **3.2 技术目标**：前端实现层面的技术策略（组件复用、状态管理、跨端兼容等）
- **3.3 性能指标**：若 PRD 或技术方案中有明确性能要求则写入，否则可省略该节
- **3.4 其他指标**：兼容性要求、配置开关、代码规范要求等

---

### 4 系统环境

- **4.1 本功能所处的环境**：所属业务模块、宿主应用（App/小程序）、运行平台、涉及页面、部署环境
- **4.2 相关软件及硬件**：前端框架、构建工具、客户端系统版本、网络环境

---

### 5 设计思路和方案

> **简单需求判断原则**：若 PRD 涉及页面少于 2 个、无复杂跳转链路、无多端适配，可跳过 5.1 整体流程图和 5.2 页面整体结构，直接进入第 6 章详细设计。

#### 5.1 整体流程图（按需）
- 使用 mermaid 语法书写流程图
- **最终写入文档时，必须转换为 mermaid.ink 图片链接格式**，不得保留原始 mermaid 代码块
- 转换方式：对 mermaid 代码字符串做 URL-safe base64 编码（`+`→`-`，`/`→`_`，去掉 `=`），拼接 `https://mermaid.ink/img/<encoded>`

```markdown
![流程图描述](https://mermaid.ink/img/<url-safe-base64>)
```

#### 5.2 页面整体结构（按需）
- 描述各页面的层级关系与核心区块组成

#### 5.3 整体项目难点梳理与解决方案
- 使用 Markdown 标准表格，**禁止使用 HTML 标签格式**
- 表头：难点描述 / 解决方案
- 重点列出前端实现中有一定复杂度的技术点（如定时器管理、横竖屏适配、跨端路由等）

#### 5.4 跨部门协同方案（按需）
- 若涉及后端接口约定、产品字段新增、埋点数据对齐，需分角色说明协同事项

---

### 6 详细设计

#### 6.1 页面结构说明
- 按页面/区域逐一描述，每个区块说明：UI 结构、交互逻辑、数据来源
- **每个页面区块必须附带代码实现关键点**，格式如下：

```typescript
// 文件路径注释必须写在代码块首行，如：
// pages/middle-recommend/index.tsx

// 改动点描述注释（说明修改了什么，为什么改）
// 原代码（用注释保留，便于对比）：
// <原有实现>

// 新代码：
<修改后的实现>
```

- 代码注释规范：
  - 改动点用中文注释说明修改意图
  - 删除的代码用注释保留（加 `// 已移除：` 前缀），不直接删除
  - 新增代码标注 `// 新增：` 或 `// 改造：`

- **若该页面无实际代码变更（纯产品配置/文案调整）**，则提供伪代码描述预期实现逻辑

#### 6.2 通用代码设计
- **仅关注不属于 `pages/` 和 `pageComponent/` 下的代码**
- 重点关注范围：`utils/`、`components/`、`hooks/`、`context/`、`store/`、`service/`、`interface/`、`constants/`
- 对每个新增/改造的通用模块，需明确说明：
  - 组件/Hook/工具函数的职责边界
  - 是否为**通用组件**（跨业务复用）还是**业务组件**（仅当前模块使用）
  - 与业务层的解耦方式（如 children 插槽、props 传入、context 注入等）
- 代码示例必须包含：
  - TypeScript 类型定义（Interface/Type）
  - Service 层封装（若有新增接口）
  - 完整的组件/Hook 实现（关键逻辑部分）

#### 6.3 接口描述
- **使用 Markdown 标准表格，禁止使用 HTML 标签格式**
- 表头：隶属组件/页面 / 接口说明 / 接口路径 / 参数 / 返回值
- 参数和返回值字段使用代码块内嵌方式，写入 TypeScript 类型（若代码中已定义则引用，若无则根据接口语义推断写一版）

示例：

| 隶属组件/页面 | 接口说明 | 接口路径 | 参数 | 返回值 |
| --- | --- | --- | --- | --- |
| 题目解析页 | 获取折叠操作栏按钮 | `GET /api/practice/get-collapsible-action-bar-content` | `{ gradeTermId: string }` | `{ guideButtons: IFunctionEntries[] }` |

- 区分**新增接口**与**已有接口改造**，分开列示

#### 6.4 测试考虑
- 按页面列出测试用例，重点覆盖：
  - 空数据/异常状态展示
  - 分页/加载更多边界
  - 多端（iOS / Android / 微信小程序）兼容性
  - 服务端配置开关的开/关状态

---

### 7 埋点信息

- **使用 Markdown 标准表格，禁止使用 HTML 标签格式**
- 表头：事件名称 / 事件ID / 事件描述 / 触发时机 / 触发页面 / 参数列表
- 若埋点参数有多个，参数列表用逗号分隔（如 `question_id, type`）
- 区分**新增埋点**与**已有埋点扩展参数**

示例：

| 事件名称 | 事件ID | 事件描述 | 触发时机 | 触发页面 | 参数列表 |
| --- | --- | --- | --- | --- | --- |
| MIDDLE_RECOMMEND_ITEM_CLICK | 15977983057225728 | 好题速递卡片点击 | 题目卡片点击时上报 | 好题速递页面 | question_id |

---

## 全局格式规范

### 表格格式
- **全文禁止使用 HTML 表格标签（`<table>`、`<tr>`、`<td>`）**
- 统一使用 Markdown 标准表格语法（`| --- |`）

### 代码块规范
- 每个代码块首行必须用注释标明所属文件路径
- 语言标识符必须写明（`typescript`、`javascript`、`bash` 等）
- 代码中的改动点必须有中文注释说明改动意图

### 链接格式
- 飞书链接使用 `[文档标题](链接)` 的 Markdown 链接格式
- Figma 链接直接写完整 URL

### 章节编号
- 使用 `# 1`、`## 1.1`、`### 1.1.1` 的层级结构
- 保持全文编号连续，不跳级

---

## 同步飞书注意事项

> 编写完 MD 文档后，若需同步到飞书文档，参考 `.cursor/skills/write-fe-reverse-teaching-doc/SKILL.md` 中的完整流程。

关键注意点：
- HTML 表格在 `docx_v1_document_convert` 转换后会产生 `merge_info` 字段，插入时必须先清除（这也是为什么 MD 文档本身禁止使用 HTML 表格）
- 分批插入时每批不超过 12 个顶层块，JSON 不超过 25KB
- mermaid.ink 链接必须使用 URL-safe base64（`+`→`-`、`/`→`_`、去掉 `=`）
